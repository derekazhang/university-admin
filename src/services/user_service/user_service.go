package user_service

import "os/user"

// "apis/src/models/user"

func WxCodeLogin(code string) (ret user.User, err error) {
	// authRet, err : =wx.WxCodeAuth(code)
	// mini := wc.GetMiniProgram(cfg)
	// if err != nil {
	// 	return
	// }
	// if authRet.Openid == "" {
	// 	err = errors.New(("注册失败"))
	// 	return
	// }
	return
}
